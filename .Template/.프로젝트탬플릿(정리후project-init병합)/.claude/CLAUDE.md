# CLAUDE.md

## 세션 시작

> SessionStart 훅이 세션 시작 시 아래 3개 파일 읽기를 강제한다
> Bash/Edit/Write는 3개 파일 Read 완료 전까지 기술적으로 차단된다

강제 읽기 순서:
1. 마스터플랜 (`.claude/plans/*-master.md`)
2. 현재 활성 페이지플랜 — `## 작업 항목` 섹션
3. `.claude/WORKSPACE.md` — `## 현재 위치` + `## 남은 과제`

필요 시 추가로 읽는 파일:
- `.claude/CONTEXT.md` — 프로젝트 구조
- `.claude/RULES.md` — 작업 원칙

## 페이지플랜 규칙

- 모든 페이지플랜의 현재 작업 섹션 이름: `## 작업 항목` (고정)
- 마스터플랜 상단 `**현재 활성 플랜:** \`파일명\`` 으로 활성 플랜 지정
- 페이지 전환 시: 마스터플랜 포인터 수정 + WORKSPACE.md 초기화

## 프로젝트 정체성

**재정계획 스킬 — 지자체 중기지방재정계획 분석 스킬 개발**
Claude CLI 환경에서 동작하는 챗봇형 스킬. 사용자와 단계별 대화를 통해 지자체별 분석용 파일을 생성한다.

스킬 트리거: `/재정계획`
등록 위치: `.claude/commands/재정계획.md`
실행 환경: Claude Code (CLI)

**이 프로젝트의 범위**: 스킬 제작. 실제 재정 분석은 스킬 실행 후 생성된 폴더(`재정계획-{지자체명}{순번}/`)에서 별도로 진행한다.

---

## 현재 진행 방향

> 현재 Phase, 플랜, 상태는 **`.claude/WORKSPACE.md`**에서 확인한다.
> 전체 Phase 구조와 완료 조건은 **마스터플랜**에서 확인한다.

마스터플랜: `.claude/plans/재정계획-master.md`

---

## 절대 금지

- 플랜에 없는 파일 신규 생성 금지
- 현재 Phase 범위를 초과한 작업 금지
- 스킬 파일에 한글 대신 한자 사용 금지

---

## 파일 내용 보고 원칙 (필수)

파일을 읽고 내용을 보고하거나 분석할 때:

- **파일에 실제로 있는 내용만 말한다**
- 파일에 없는 내용을 "누락", "추가로", "참고로" 등의 표현으로 덧붙이는 것 **금지**
- 추론·추정·배경지식을 파일 내용인 것처럼 섞어서 말하는 것 **금지**
- 파일 외 정보를 언급해야 할 경우 반드시 **(파일 외 정보)** 라고 명시한 후 사용자 확인을 받는다
- 확인하지 않은 사실을 단정적으로 말하는 것 **금지** — 불확실하면 "확인이 필요합니다"라고 말한다

---

## WORKSPACE.md 운용 원칙 (필수)

**파일 구조 (3섹션 고정)**
- `## 현재 위치` — 항상 읽음
- `## 남은 과제` — 항상 읽음
- `## 진행 이력` — 필요할 때만 추가 요청

**기본 규칙**
- **Edit 도구로만 수정**: 내용 추가·수정은 자유롭게 허용
- **Write 도구 차단**: 훅이 자동으로 덮어쓰기를 막는다
- **체크박스 없음**: 진행상황은 서술 방식으로 기록. 체크박스는 페이지 플랜에만 존재
- **진행 이력**: 최신→과거 순으로 추가

**새 WORKSPACE.md가 필요한 경우 (새 Phase 시작 등)**
1. 기존 WORKSPACE.md를 이름 변경: `WORKSPACE.{플랜명}.md`
2. 새 WORKSPACE.md 작성 (훅이 이름 변경 확인 후 허용)

**보류 항목**
- 완료되지 않은 항목은 새 WORKSPACE.md에도 보류 섹션으로 계속 포함

**금지**
- 사용자 지시 없이 WORKSPACE.md 내용 삭제 금지
- 체크박스 형식 사용 금지

---

## 작업 순서 원칙 (필수 — 반드시 이 순서로)

```
1. 플랜 파일에서 해당 항목 확인 (없으면 먼저 추가)
2. 플랜 항목 상태 🔲 → 🔄 (진행중) 표시
3. 구현
4. 플랜 항목 상태 🔄 → ✅ 표시
5. WORKSPACE.md 진행상황 업데이트
6. 커밋
```

**금지**: 구현 먼저 → 나중에 플랜 업데이트
**금지**: 플랜 업데이트 없이 다음 항목으로 이동

---

## 작업 완료 체크리스트

- [ ] RULES.md 원칙 준수
- [ ] 플랜 체크박스 업데이트
- [ ] WORKSPACE.md 진행상황 업데이트
- [ ] 현재 플랜 완료 조건 충족 여부 확인
