# 06. 슬래시 명령어

**주요 명령어 분류**

- **세션 관리**: `/clear`, `/compact`, `/stats`.
- **개발 도구**: `/test`, `/lint`, `/review`.
- **유틸리티**: `/undo`, `/copy`, `/help`.
- **환경 이동**: `/desktop`, `/teleport`.

**퀵 입력 접두사 (Quick Prefixes)**

- `>` : Bash 명령어 실행
- `?` : 웹 검색 수행
- `!` : 파일 내용 읽기
